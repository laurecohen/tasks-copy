{{ form_start(form) }}
    
    <div class="form-inline form-info">
        <div>{{ form_errors(form.label) }}</div>
        <div class="form-group">
            {{ form_widget(form.label) }}
        </div>
        <button class="btn btn-sm btn-outline-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}